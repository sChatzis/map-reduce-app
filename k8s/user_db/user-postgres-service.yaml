apiVersion: v1
kind: Service
metadata:
  name: user-db
  namespace: mapreduce
  labels:
    app: user-db
spec:
  clusterIP: None
  selector:
    app: user-db
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
